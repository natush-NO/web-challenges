import StyledButton, { PrimaryButton } from "./components/StyledButton";

function App() {
  return (
    <main>
      <h1>Hello from React</h1>
      <StyledButton>Default Button </StyledButton>
      <PrimaryButton>Primary Button</PrimaryButton>
    </main>
  );
}

export default App;
